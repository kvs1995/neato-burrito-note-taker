//require fs and util

//promise version of fs.readFile
  //function to write data to the JSON file given a desctination and some content

    //the file you want to write to
    //the content you want to write to the file

//writeToFile function, passes in the destination and content

  //function to read data from a given a file and append some content. 
    //varirable with the content you want to append to the file
    //variable of the path to the file you want to save to
  
// function to readAndAppend, passes in the content and file
  //read file and takes the parsedData and pushes it to the content then writes the parsed Data to the file itself.


//export readFromFile, writeToFile, readAndAppend modules